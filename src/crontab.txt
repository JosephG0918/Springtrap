# Runs the follower.py script
@reboot sleep 5 && /home/<user>/PythonDir/SpringtrapCodebase/<venv>/bin/python /home/<user>/PythonDir/SpringtrapCodebase/follower.py > /home/<user>/logs/cron-follower.log 2>&1

# Runs the shutdown-press-simple.py script
@reboot sleep 5 && /home/<user>/PythonDir/SpringtrapCodebase/<venv>/bin/python /home/<user>/PythonDir/SpringtrapCodebase/shutdown_button/shutdown-press-simple.py > /home/<user>/logs/cron-shutdown.log 2>&1